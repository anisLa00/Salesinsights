# ── Web framework ────────────────────────────────────────────
fastapi==0.141.1
uvicorn==0.52.0
python-multipart==0.0.32

# ── Database: SQLModel + async SQLAlchemy + PostgreSQL ───────
sqlmodel==0.0.39
SQLAlchemy==2.0.51
asyncpg==0.31.0
alembic==1.19.0

# ── Config & validation ─────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
email-validator==2.3.0

# ── Auth (JWT + password hashing) ───────────────────────────
PyJWT==2.13.0
passlib==1.7.4
bcrypt==4.0.1
itsdangerous==2.2.0

# ── Background tasks & cache (Celery + Redis) ───────────────
celery==5.6.3
redis==6.4.0
flower==2.1.0
asgiref==3.12.1

# ── Email (verification / password reset) ───────────────────
fastapi-mail==1.6.5

# ── AI (Claude — powers the sales insights) ─────────────────
anthropic==0.42.0

# ── Testing ─────────────────────────────────────────────────
pytest==9.1.1
httpx==0.28.1
